name: Judge console

# Publishes the read-only judge console. The site is static and carries its
# evidence with it, so what ships cannot reach a model, spend quota, or start an
# execution -- it keeps working through an outage, a cold start, and a hundred
# refreshes on a judging laptop.

on:
  push:
    branches: [main]
    paths: ["web/**", "artifacts/**", "scripts/**", ".github/workflows/pages.yml"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # Without the full dependency set, unittest reports import failures as
      # tests and the suite silently describes the runner instead of the code.
      - name: Install dependencies
        run: python -m pip install -r requirements.txt

      # The bundle is derived from committed evidence. Rebuilding here means a
      # page that disagrees with its own executions never reaches the web.
      - name: Rebuild the proof bundle from committed evidence
        run: python scripts/build_proof_bundle.py

      - name: Fail if the committed bundle drifted from the evidence
        run: git diff --exit-code -- web/proof-bundle.json

      # The MP4 is silent. Native browser captions require WebVTT wired through
      # a <track> element; an SRT download link alone cannot populate the CC menu.
      # This hosted-only transform runs after the evidence drift check and is
      # stripped from the offline single-file console by the existing builder.
      - name: Wire native English captions into the hosted demo
        run: python scripts/inject_demo_captions.py

      - name: Build the self-contained page
        run: python scripts/build_single_file.py

      - name: Check the console cannot start work or leak secrets
        run: python -m unittest tests.test_judge_console tests.test_demo_captions -v

      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: web

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
