google-adk>=2.7,<3
# Used directly by proofos_service, so pinned explicitly rather than relied on
# as a transitive dependency of google-adk.
fastapi>=0.115,<1
uvicorn>=0.30,<1
# Durable journal sink. Imported lazily, so the in-memory backend still works
# if this is absent.
google-cloud-firestore>=2.20,<3
# Used directly by the compose-configuration tests, so pinned rather than
# relied on as a transitive dependency of google-adk.
pyyaml>=6,<7
